#ifndef ZELDA_STYLE_H
#define ZELDA_STYLE_H

#include "common.h"

const sf::Color BG_COLOR = sf::Color(0, 0, 0, 180);

const int NAME_FONT_SIZE = 20;
const int TEXT_FONT_SIZE = 22;
const int CHOICE_FONT_SIZE = 20;

#endif //ZELDA_STYLE_H
